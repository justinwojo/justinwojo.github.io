---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="mb-10">
  <h2 class="mb-6 text-xl font-semibold text-[var(--color-text)] border-b border-[var(--color-border)] pb-2">
    {title}
  </h2>
  <slot />
</section>
